# Bob UI S1

OpenClaw-style command-center frontend for Mind Clone.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`
- `npm run preview`

The production build is served by FastAPI at `/ui` when `dist/` exists.
